import { Meta } from '@storybook/addon-docs';
import {Tokens} from '../../components/tokens-grid.tsx'
import { fontSizes } from '@ignite-ui/tokens'

<Meta title="Tokens/Fonts sizes"/>

# Fonts sizes
<Tokens token={fontSizes} hasRemValue={true} />
