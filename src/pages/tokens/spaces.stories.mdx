import { Meta } from '@storybook/addon-docs';
import {Tokens} from '../../components/tokens-grid.tsx'
import { space } from '@ignite-ui/tokens'

<Meta title="Tokens/Spaces"/>

# Spaces
<Tokens token={space} hasRemValue={true} />
